<p>The <strong>http_status_code</strong> function in mock-jutsu generates common http status code (2xx/3xx/4xx/5xx). This generator belongs to the <em>Web</em> category and produces realistic mock data for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('http_status_code')</code> in Python or <code>mockjutsu generate http_status_code</code> from the CLI to get http status code mock data. Developers working with Web systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>403</code>. The mock-jutsu http_status_code generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test data as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(http_status_code,)}</code> to generate realistic http status code values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>