<p>The <strong>pep_status</strong> function in mock-jutsu generates politically exposed person status: not pep, pep, rca, former pep, unknown. This generator belongs to the <em>Compliance</em> category and produces realistic mock veri for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('pep_status')</code> in Python or <code>mockjutsu generate pep_status</code> from the CLI to get pep status mock veri. Geliştiriciler working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>Unknown</code>. The mock-jutsu pep_status generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test verisi as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(pep_status,)}</code> to generate realistic pep status values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>