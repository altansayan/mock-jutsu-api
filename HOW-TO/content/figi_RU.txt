<p>The <strong>figi</strong> function in mock-jutsu generates financial instrument global identifier (openfigi): 2-letter + g + 8 nsin + 1 luhn check. This generator belongs to the <em>CapMarkets(Trading)</em> category and produces realistic mock-данные for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('figi')</code> in Python or <code>mockjutsu generate figi</code> from the CLI to get figi mock-данные. Разработчики working with CapMarkets(Trading) systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>BVGL1P5ZX9G7</code>. The mock-jutsu figi generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic тестовые данные as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(figi,)}</code> to generate realistic figi values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>