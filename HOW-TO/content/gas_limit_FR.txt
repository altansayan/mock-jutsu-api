<p>The <strong>gas_limit</strong> function in mock-jutsu generates ethereum transaction gas limit — standard values (21000–1m). This generator belongs to the <em>Crypto</em> category and produces realistic données mock for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('gas_limit')</code> in Python or <code>mockjutsu generate gas_limit</code> from the CLI to get gas limit données mock. Développeurs working with Crypto systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>500000</code>. The mock-jutsu gas_limit generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic données de test as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(gas_limit,)}</code> to generate realistic gas limit values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>