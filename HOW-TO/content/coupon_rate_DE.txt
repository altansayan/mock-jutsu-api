<p>The <strong>coupon_rate</strong> function in mock-jutsu generates bond coupon rate as percentage string (0.00–12.00%). This generator belongs to the <em>CapMarkets(Trading)</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('coupon_rate')</code> in Python or <code>mockjutsu generate coupon_rate</code> from the CLI to get coupon rate Mock-Daten. Entwickler working with CapMarkets(Trading) systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>5.29</code>. The mock-jutsu coupon_rate generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(coupon_rate,)}</code> to generate realistic coupon rate values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>