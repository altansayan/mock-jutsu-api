<p>The <strong>portfolio_id_masked</strong> function in mock-jutsu generates masked portfolio id — last 4 chars of suffix visible (mifid ii art. 25 internal reference). This generator belongs to the <em>CapMarkets(Trading)</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('portfolio_id_masked')</code> in Python or <code>mockjutsu generate portfolio_id_masked</code> from the CLI to get portfolio id masked Mock-Daten. Entwickler working with CapMarkets(Trading) systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>PRTF-****JUCE</code>. The mock-jutsu portfolio_id_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(portfolio_id_masked,)}</code> to generate realistic portfolio id masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>