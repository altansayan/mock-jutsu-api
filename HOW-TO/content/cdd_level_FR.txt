<p>The <strong>cdd_level</strong> function in mock-jutsu generates customer due diligence level: standard, enhanced, simplified. This generator belongs to the <em>Compliance</em> category and produces realistic données mock for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('cdd_level')</code> in Python or <code>mockjutsu generate cdd_level</code> from the CLI to get cdd level données mock. Développeurs working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>Simplified</code>. The mock-jutsu cdd_level generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic données de test as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(cdd_level,)}</code> to generate realistic cdd level values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>