<p>The <strong>bsb_code</strong> function in mock-jutsu generates australian bsb code: nnn-nnn (bank/state prefix + branch number). This generator belongs to the <em>Banking</em> category and produces realistic mock dani for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('bsb_code')</code> in Python or <code>mockjutsu generate bsb_code</code> from the CLI to get bsb code mock dani. Rozrobnyky working with Banking systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>081-885</code>. The mock-jutsu bsb_code generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic testovi dani as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(bsb_code,)}</code> to generate realistic bsb code values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>