<p>The <strong>onboarding_method</strong> function in mock-jutsu generates customer onboarding method: ekyc, video kyc, in-branch, document upload, biometric, agent. This generator belongs to the <em>Compliance</em> category and produces realistic mock dani for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('onboarding_method')</code> in Python or <code>mockjutsu generate onboarding_method</code> from the CLI to get onboarding method mock dani. Rozrobnyky working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>Agent</code>. The mock-jutsu onboarding_method generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic testovi dani as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(onboarding_method,)}</code> to generate realistic onboarding method values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>