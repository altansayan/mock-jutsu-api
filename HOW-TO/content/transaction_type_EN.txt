<p>The <strong>transaction_type</strong> function in mock-jutsu generates transaction type code: credit, debit, transfer, refund, reversal, chargeback, fee, interest. This generator belongs to the <em>Banking</em> category and produces realistic mock data for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('transaction_type')</code> in Python or <code>mockjutsu generate transaction_type</code> from the CLI to get transaction type mock data. Developers working with Banking systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>INTEREST</code>. The mock-jutsu transaction_type generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test data as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(transaction_type,)}</code> to generate realistic transaction type values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>