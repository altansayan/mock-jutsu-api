<p>The <strong>transaction_description</strong> function in mock-jutsu generates locale-aware payment/transfer description. This generator belongs to the <em>Banking</em> category and produces realistic mock veri for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('transaction_description')</code> in Python or <code>mockjutsu generate transaction_description --locale TR</code> from the CLI to get transaction description mock veri. Geliştiriciler working with Banking systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>Kira ödemesi</code>. The mock-jutsu transaction_description generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test verisi as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(transaction_description,)}</code> to generate realistic transaction description values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>