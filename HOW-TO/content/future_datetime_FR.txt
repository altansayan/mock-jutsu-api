<p>The <strong>future_datetime</strong> function in mock-jutsu generates random iso 8601 datetime in the future (up to 5 years ahead). This generator belongs to the <em>Datetime</em> category and produces realistic données mock for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('future_datetime')</code> in Python or <code>mockjutsu generate future_datetime</code> from the CLI to get future datetime données mock. Développeurs working with Datetime systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>2030-05-18T04:23:32</code>. The mock-jutsu future_datetime generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic données de test as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(future_datetime,)}</code> to generate realistic future datetime values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>