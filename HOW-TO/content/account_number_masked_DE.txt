<p>The <strong>account_number_masked</strong> function in mock-jutsu generates masked account number — last 4 digits visible (pci-dss v4.0 §3.3). This generator belongs to the <em>Banking</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('account_number_masked')</code> in Python or <code>mockjutsu generate account_number_masked</code> from the CLI to get account number masked Mock-Daten. Entwickler working with Banking systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>****9639</code>. The mock-jutsu account_number_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(account_number_masked,)}</code> to generate realistic account number masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>