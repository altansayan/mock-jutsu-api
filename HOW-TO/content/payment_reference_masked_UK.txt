<p>The <strong>payment_reference_masked</strong> function in mock-jutsu generates payment reference with sequence masked — date segment visible (glba best practice). This generator belongs to the <em>Banking</em> category and produces realistic mock dani for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('payment_reference_masked')</code> in Python or <code>mockjutsu generate payment_reference_masked</code> from the CLI to get payment reference masked mock dani. Rozrobnyky working with Banking systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>PAYREF-20260620-*****</code>. The mock-jutsu payment_reference_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic testovi dani as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(payment_reference_masked,)}</code> to generate realistic payment reference masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>