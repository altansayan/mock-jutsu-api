<p>The <strong>forex_rate</strong> function in mock-jutsu generates realistic fx exchange rate (4 decimal places) for major currency pairs. This generator belongs to the <em>CapMarkets(Trading)</em> category and produces realistic mock data for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('forex_rate')</code> in Python or <code>mockjutsu generate forex_rate</code> from the CLI to get forex rate mock data. Developers working with CapMarkets(Trading) systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>129.2924</code>. The mock-jutsu forex_rate generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test data as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(forex_rate,)}</code> to generate realistic forex rate values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>