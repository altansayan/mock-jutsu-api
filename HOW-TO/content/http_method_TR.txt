<p>The <strong>http_method</strong> function in mock-jutsu generates standard http verb (get, post, put, patch, delete, head, options). This generator belongs to the <em>Web</em> category and produces realistic mock veri for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('http_method')</code> in Python or <code>mockjutsu generate http_method</code> from the CLI to get http method mock veri. Geliştiriciler working with Web systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>DELETE</code>. The mock-jutsu http_method generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test verisi as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(http_method,)}</code> to generate realistic http method values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>