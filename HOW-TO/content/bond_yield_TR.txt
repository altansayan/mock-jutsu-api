<p>The <strong>bond_yield</strong> function in mock-jutsu generates bond yield as percentage string (0.01–15.00%). This generator belongs to the <em>CapMarkets(Trading)</em> category and produces realistic mock veri for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('bond_yield')</code> in Python or <code>mockjutsu generate bond_yield</code> from the CLI to get bond yield mock veri. Geliştiriciler working with CapMarkets(Trading) systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>7.79</code>. The mock-jutsu bond_yield generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test verisi as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(bond_yield,)}</code> to generate realistic bond yield values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>