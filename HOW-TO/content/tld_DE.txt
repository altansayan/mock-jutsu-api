<p>The <strong>tld</strong> function in mock-jutsu generates top-level domain extension (.com, .net, .io, .dev, etc.). This generator belongs to the <em>Web</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('tld')</code> in Python or <code>mockjutsu generate tld</code> from the CLI to get tld Mock-Daten. Entwickler working with Web systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>.cloud</code>. The mock-jutsu tld generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(tld,)}</code> to generate realistic tld values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>