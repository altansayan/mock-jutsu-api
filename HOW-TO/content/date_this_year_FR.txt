<p>The <strong>date_this_year</strong> function in mock-jutsu generates random date within the current calendar year. This generator belongs to the <em>Datetime</em> category and produces realistic données mock for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('date_this_year')</code> in Python or <code>mockjutsu generate date_this_year</code> from the CLI to get date this year données mock. Développeurs working with Datetime systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>2026-09-01</code>. The mock-jutsu date_this_year generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic données de test as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(date_this_year,)}</code> to generate realistic date this year values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>