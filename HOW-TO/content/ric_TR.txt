<p>The <strong>ric</strong> function in mock-jutsu generates reuters instrument code: ticker.exchange suffix per locale. This generator belongs to the <em>CapMarkets(Trading)</em> category and produces realistic mock veri for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('ric')</code> in Python or <code>mockjutsu generate ric --locale US</code> from the CLI to get ric mock veri. Geliştiriciler working with CapMarkets(Trading) systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>NEE.IS</code>. The mock-jutsu ric generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test verisi as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(ric,)}</code> to generate realistic ric values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>