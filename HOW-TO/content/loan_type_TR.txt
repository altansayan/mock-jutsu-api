<p>The <strong>loan_type</strong> function in mock-jutsu generates loan product type: personal, mortgage, auto, student, business, home equity, payday. This generator belongs to the <em>FinancialExt</em> category and produces realistic mock veri for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('loan_type')</code> in Python or <code>mockjutsu generate loan_type</code> from the CLI to get loan type mock veri. Geliştiriciler working with FinancialExt systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>Auto</code>. The mock-jutsu loan_type generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test verisi as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(loan_type,)}</code> to generate realistic loan type values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>