<p>The <strong>consent_id</strong> function in mock-jutsu generates regulatory consent identifier: uuid v4 or consent- prefixed string. This generator belongs to the <em>Compliance</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('consent_id')</code> in Python or <code>mockjutsu generate consent_id</code> from the CLI to get consent id Mock-Daten. Entwickler working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>d940d99d-8491-4fc8-9c92-154e739bd708</code>. The mock-jutsu consent_id generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(consent_id,)}</code> to generate realistic consent id values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>