<p>The <strong>policy_number_masked</strong> function in mock-jutsu generates masked policy number — sequence visible for support reference (glba §501 npi). This generator belongs to the <em>Compliance</em> category and produces realistic mock data for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('policy_number_masked')</code> in Python or <code>mockjutsu generate policy_number_masked</code> from the CLI to get policy number masked mock data. Developers working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>POL-****-33961</code>. The mock-jutsu policy_number_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test data as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(policy_number_masked,)}</code> to generate realistic policy number masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>