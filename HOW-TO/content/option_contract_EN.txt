<p>The <strong>option_contract</strong> function in mock-jutsu generates occ option contract symbol: ticker + yymmdd + c/p + 8-digit strike (cents). This generator belongs to the <em>CapMarkets(Trading)</em> category and produces realistic mock data for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('option_contract')</code> in Python or <code>mockjutsu generate option_contract</code> from the CLI to get option contract mock data. Developers working with CapMarkets(Trading) systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>JNJ261104P04842200</code>. The mock-jutsu option_contract generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test data as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(option_contract,)}</code> to generate realistic option contract values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>