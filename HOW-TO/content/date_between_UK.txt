<p>The <strong>date_between</strong> function in mock-jutsu generates random date between two dates (default: past 5 years to today). This generator belongs to the <em>Datetime</em> category and produces realistic mock dani for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('date_between')</code> in Python or <code>mockjutsu generate date_between</code> from the CLI to get date between mock dani. Rozrobnyky working with Datetime systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>2023-12-01</code>. The mock-jutsu date_between generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic testovi dani as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(date_between,)}</code> to generate realistic date between values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>