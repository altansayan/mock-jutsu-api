<p>The <strong>credit_limit_masked</strong> function in mock-jutsu generates masked credit limit — order of magnitude hidden (glba §501 npi). This generator belongs to the <em>FinancialExt</em> category and produces realistic mock veri for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('credit_limit_masked')</code> in Python or <code>mockjutsu generate credit_limit_masked</code> from the CLI to get credit limit masked mock veri. Geliştiriciler working with FinancialExt systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>$**,***</code>. The mock-jutsu credit_limit_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test verisi as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(credit_limit_masked,)}</code> to generate realistic credit limit masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>