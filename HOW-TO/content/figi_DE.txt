<p>The <strong>figi</strong> function in mock-jutsu generates financial instrument global identifier (openfigi): 2-letter + g + 8 nsin + 1 luhn check. This generator belongs to the <em>CapMarkets(Trading)</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('figi')</code> in Python or <code>mockjutsu generate figi</code> from the CLI to get figi Mock-Daten. Entwickler working with CapMarkets(Trading) systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>BPGPJL1LN2N4</code>. The mock-jutsu figi generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(figi,)}</code> to generate realistic figi values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>