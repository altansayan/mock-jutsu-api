<p>The <strong>transaction_description_masked</strong> function in mock-jutsu generates locale-aware payment description truncated to 10 chars + *** (gdpr art. 5(1)(c) data minimisation). This generator belongs to the <em>Banking</em> category and produces realistic mock data for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('transaction_description_masked')</code> in Python or <code>mockjutsu generate transaction_description_masked --locale TR</code> from the CLI to get transaction description masked mock data. Developers working with Banking systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>Fatura öde***</code>. The mock-jutsu transaction_description_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test data as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(transaction_description_masked,)}</code> to generate realistic transaction description masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>