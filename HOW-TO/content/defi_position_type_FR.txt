<p>The <strong>defi_position_type</strong> function in mock-jutsu generates defi position type: liquidity provider, lending, borrowing, staking, perpetual, etc. This generator belongs to the <em>Crypto</em> category and produces realistic données mock for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('defi_position_type')</code> in Python or <code>mockjutsu generate defi_position_type</code> from the CLI to get defi position type données mock. Développeurs working with Crypto systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>Perpetual</code>. The mock-jutsu defi_position_type generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic données de test as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(defi_position_type,)}</code> to generate realistic defi position type values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>