<p>The <strong>premium_amount</strong> function in mock-jutsu generates insurance premium amount: $25.00–$2,500.00 (monthly). This generator belongs to the <em>FinancialExt</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('premium_amount')</code> in Python or <code>mockjutsu generate premium_amount</code> from the CLI to get premium amount Mock-Daten. Entwickler working with FinancialExt systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>1960.15</code>. The mock-jutsu premium_amount generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(premium_amount,)}</code> to generate realistic premium amount values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>