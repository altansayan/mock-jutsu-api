<p>The <strong>premium_amount_masked</strong> function in mock-jutsu generates masked insurance premium — exact amount hidden (glba §501 npi). This generator belongs to the <em>FinancialExt</em> category and produces realistic mock dani for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('premium_amount_masked')</code> in Python or <code>mockjutsu generate premium_amount_masked</code> from the CLI to get premium amount masked mock dani. Rozrobnyky working with FinancialExt systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>$*,***</code>. The mock-jutsu premium_amount_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic testovi dani as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(premium_amount_masked,)}</code> to generate realistic premium amount masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>