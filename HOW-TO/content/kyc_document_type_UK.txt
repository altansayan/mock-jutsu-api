<p>The <strong>kyc_document_type</strong> function in mock-jutsu generates kyc accepted document type: passport, national id, drivers license, etc. This generator belongs to the <em>Compliance</em> category and produces realistic mock dani for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('kyc_document_type')</code> in Python or <code>mockjutsu generate kyc_document_type</code> from the CLI to get kyc document type mock dani. Rozrobnyky working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>Residence Permit</code>. The mock-jutsu kyc_document_type generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic testovi dani as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(kyc_document_type,)}</code> to generate realistic kyc document type values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>