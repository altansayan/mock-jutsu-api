<p>The <strong>mortgage_rate</strong> function in mock-jutsu generates mortgage interest rate as percentage string (1.50–12.00%). This generator belongs to the <em>FinancialExt</em> category and produces realistic données mock for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('mortgage_rate')</code> in Python or <code>mockjutsu generate mortgage_rate</code> from the CLI to get mortgage rate données mock. Développeurs working with FinancialExt systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>6.36</code>. The mock-jutsu mortgage_rate generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic données de test as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(mortgage_rate,)}</code> to generate realistic mortgage rate values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>