<p>The <strong>ubo_ownership_percentage</strong> function in mock-jutsu generates ultimate beneficial owner ownership percentage (0.01–100.00). This generator belongs to the <em>Compliance</em> category and produces realistic mock data for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('ubo_ownership_percentage')</code> in Python or <code>mockjutsu generate ubo_ownership_percentage</code> from the CLI to get ubo ownership percentage mock data. Developers working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>38.18</code>. The mock-jutsu ubo_ownership_percentage generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test data as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(ubo_ownership_percentage,)}</code> to generate realistic ubo ownership percentage values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>