<p>The <strong>credit_utilization</strong> function in mock-jutsu generates credit utilization ratio as percentage string (0.00–100.00). This generator belongs to the <em>FinancialExt</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('credit_utilization')</code> in Python or <code>mockjutsu generate credit_utilization</code> from the CLI to get credit utilization Mock-Daten. Entwickler working with FinancialExt systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>90.49</code>. The mock-jutsu credit_utilization generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(credit_utilization,)}</code> to generate realistic credit utilization values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>