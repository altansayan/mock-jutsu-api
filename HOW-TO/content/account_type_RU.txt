<p>The <strong>account_type</strong> function in mock-jutsu generates bank account type: checking, savings, current, business, money market, cd, investment. This generator belongs to the <em>Banking</em> category and produces realistic mock-данные for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('account_type')</code> in Python or <code>mockjutsu generate account_type</code> from the CLI to get account type mock-данные. Разработчики working with Banking systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>CD</code>. The mock-jutsu account_type generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic тестовые данные as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(account_type,)}</code> to generate realistic account type values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>