<p>The <strong>claim_number</strong> function in mock-jutsu generates insurance claim number: clm-yyyymmdd-nnnnn. This generator belongs to the <em>Compliance</em> category and produces realistic mock-данные for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('claim_number')</code> in Python or <code>mockjutsu generate claim_number</code> from the CLI to get claim number mock-данные. Разработчики working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>CLM-20260620-24398</code>. The mock-jutsu claim_number generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic тестовые данные as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(claim_number,)}</code> to generate realistic claim number values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>