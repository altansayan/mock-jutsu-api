<p>The <strong>future_date</strong> function in mock-jutsu generates random date in the future (up to 5 years ahead), yyyy-mm-dd. This generator belongs to the <em>Datetime</em> category and produces realistic données mock for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('future_date')</code> in Python or <code>mockjutsu generate future_date</code> from the CLI to get future date données mock. Développeurs working with Datetime systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>2031-02-13</code>. The mock-jutsu future_date generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic données de test as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(future_date,)}</code> to generate realistic future date values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>