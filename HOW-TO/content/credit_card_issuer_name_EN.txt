<p>The <strong>credit_card_issuer_name</strong> function in mock-jutsu generates locale-aware fictional bank/issuer name for the credit card. This generator belongs to the <em>FinancialExt</em> category and produces realistic mock data for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('credit_card_issuer_name')</code> in Python or <code>mockjutsu generate credit_card_issuer_name --locale US</code> from the CLI to get credit card issuer name mock data. Developers working with FinancialExt systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>KırmızıBanka</code>. The mock-jutsu credit_card_issuer_name generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test data as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(credit_card_issuer_name,)}</code> to generate realistic credit card issuer name values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>