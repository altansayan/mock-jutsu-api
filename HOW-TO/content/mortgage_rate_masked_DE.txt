<p>The <strong>mortgage_rate_masked</strong> function in mock-jutsu generates masked mortgage rate — exact rate hidden (glba §501 npi). This generator belongs to the <em>FinancialExt</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('mortgage_rate_masked')</code> in Python or <code>mockjutsu generate mortgage_rate_masked</code> from the CLI to get mortgage rate masked Mock-Daten. Entwickler working with FinancialExt systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>**.**%</code>. The mock-jutsu mortgage_rate_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(mortgage_rate_masked,)}</code> to generate realistic mortgage rate masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>