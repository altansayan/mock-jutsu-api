<p>The <strong>consent_id_masked</strong> function in mock-jutsu generates masked consent id — last 8 hex chars visible for audit trail (gdpr art. 7 + eprivacy). This generator belongs to the <em>Compliance</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('consent_id_masked')</code> in Python or <code>mockjutsu generate consent_id_masked</code> from the CLI to get consent id masked Mock-Daten. Entwickler working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>****-****-****-****-32a712c9</code>. The mock-jutsu consent_id_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(consent_id_masked,)}</code> to generate realistic consent id masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>