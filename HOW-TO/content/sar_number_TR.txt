<p>The <strong>sar_number</strong> function in mock-jutsu generates suspicious activity report number: sar-yyyymmdd-nnnnn. This generator belongs to the <em>Compliance</em> category and produces realistic mock veri for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('sar_number')</code> in Python or <code>mockjutsu generate sar_number</code> from the CLI to get sar number mock veri. Geliştiriciler working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>SAR-20260620-706455</code>. The mock-jutsu sar_number generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test verisi as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(sar_number,)}</code> to generate realistic sar number values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>