<p>The <strong>cve_id</strong> function in mock-jutsu generates mitre cve identifier: cve-yyyy-nnnnn (year 2000–2025). This generator belongs to the <em>Security</em> category and produces realistic mock data for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('cve_id')</code> in Python or <code>mockjutsu generate cve_id</code> from the CLI to get cve id mock data. Developers working with Security systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>CVE-2007-12353</code>. The mock-jutsu cve_id generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test data as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(cve_id,)}</code> to generate realistic cve id values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>