<p>The <strong>micr_line_masked</strong> function in mock-jutsu generates micr line with account segment masked — routing number remains visible (public aba directory). This generator belongs to the <em>Banking</em> category and produces realistic mock data for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('micr_line_masked')</code> in Python or <code>mockjutsu generate micr_line_masked</code> from the CLI to get micr line masked mock data. Developers working with Banking systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>|647617652| |****| 1884</code>. The mock-jutsu micr_line_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic test data as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(micr_line_masked,)}</code> to generate realistic micr line masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>