<p>The <strong>ubo_ownership_percentage_masked</strong> function in mock-jutsu generates masked ubo ownership percentage — exact stake hidden (eu 4amld/5amld art. 30). This generator belongs to the <em>Compliance</em> category and produces realistic Mock-Daten for testing purposes without touching production systems.</p>

<p>Use <code>jutsu.generate('ubo_ownership_percentage_masked')</code> in Python or <code>mockjutsu generate ubo_ownership_percentage_masked</code> from the CLI to get ubo ownership percentage masked Mock-Daten. Entwickler working with Compliance systems benefit from this mock generator in unit tests, integration tests, and CI pipelines.</p>

<p>Sample output: <code>**%</code>. The mock-jutsu ubo_ownership_percentage_masked generator follows real-world format specifications, ensuring that validation logic and downstream parsers behave identically with synthetic Testdaten as they would with real data.</p>

<p>For JMeter performance tests use <code>${__mockjutsu(ubo_ownership_percentage_masked,)}</code> to generate realistic ubo ownership percentage masked values at scale. mock-jutsu has zero runtime dependencies — all generation happens locally in pure Python, making it safe and fast for any CI environment.</p>